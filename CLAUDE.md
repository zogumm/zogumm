# zogumm

건축 법규검토 도구. 대지 조건으로 **최대 건축 가능 규모**를 판정한다.

목적은 준법 검사가 아니라 **면적 최대화**다. "이 땅에 얼마나, 어떤 용도로 지을 수 있나"가
1번 질문이고, 공사비·시공성은 나중 문제다. 이 방향이 코드의 여러 판단을 결정한다.

주 작업 디렉터리는 `lawkit/`. (`css-navbar-09/` 는 무관한 별개 습작이다.)

---

## 왜 시작됐나 — 자곡동 271-4 건폐율 20% 사고

기존 도구가 서울 강남구 자곡동 271-4 의 건폐율을 **20%** 로 냈다. 실제로는 **60%** 였다.

- 대지: 331㎡ / **자연녹지지역 + 개발제한구역 + 취락지구**
- 도구 출력: 건폐율 20%, 건축면적 66.2㎡, 연면적 165.5㎡
- 실제: 건폐율 60%, 건축면적 198.6㎡, **연면적 300㎡**

원인은 데이터 부재가 아니었다. 도구는 "구역 2건", "근거법령 58건(개발제한구역·취락지구)"
까지 화면에 표시하고 있었다. **가져온 데이터를 계산 함수가 읽지 않았다.**

```js
const 건폐율 = Math.min(법정상한, 조례)   // 20, 20 → 20
```

특별법 특례는 **완화 방향(60%)** 이라 `min` 에 넣으면 언제나 진다.
그리고 하필 **자연녹지 조례값 20% 와 GB 기본값 20% 가 같은 숫자**라, 결과만 봐서는
틀린 걸 알 수 없었다. 근거를 함께 출력하지 않으면 이 계열 버그는 계속 숨는다.

---

## 이 저장소의 원칙

이 프로젝트에서 가장 중요한 규칙이다. 코드를 고칠 때 이걸 어기지 말 것.

1. **모르면 숫자를 내지 않는다.**
   특별법 구역이 감지됐는데 매칭되는 룰이 없으면 `NEEDS_REVIEW` 로 세운다.
   조용히 용도지역 기본값으로 되돌아가는 것이 바로 20% 사고였다.

2. **모든 값은 근거를 달고 나온다.** 어느 조문에서 나왔는지 `근거[]` 에 싣는다.

3. **검증 안 된 데이터는 그 사실을 전파한다.**
   `검수상태: "미검수"` 인 룰과 `용도지역시드` 는 결과에 경고를 붙인다.

4. **용도를 모르면 확정하지 않는다.**
   GB 취락지구는 주택·근생이면 60%, 그 외는 20%. 단일 숫자 대신 `INPUT_REQUIRED`
   로 분기표를 준다.

5. **계산은 항상 최신 법으로.** 과거 법령 조회나 시점별 버전 관리는 하지 않는다
   (신축 검토가 목적이므로). 다만 룰에 `기준시행일` 을 기록해 최신인지 판별 가능하게 한다.

---

## 아키텍처 — 연산은 셋, 순서가 전부

```
gate → override → cap
```

| 연산 | 하는 일 | 누가 쓰나 |
| --- | --- | --- |
| `gate` | 트랙을 연다/닫는다 | 구역 감지 (개발제한구역 등 10종) |
| `override` | 값을 갈아끼운다 | 특별법 특례 (완화 방향) |
| `cap` | 상한을 씌운다 (`min`) | 법정상한, 조례 (강화 방향) |

**조례는 `override` 된 값에 씌우지 않는다.** 국토계획법 시행령 §84④1호가 집단취락지구를
GB법령에 통째로 넘겼고, 특별법에는 조례 계층 자체가 없다.

접도(건축법 §44)는 이 셋보다 앞이다. 2m 미만이면 건폐율이 얼마든 지을 수 없다.

### 법규의 두 축

건축법규는 **위계**(법률→시행령→조례)와 **관할**(국토계획법=땅 / 건축법=건물 /
특별법=구역)이 직교하는 2차원이다. 숫자는 시행령 별표와 조례에 있고, 법률 본문에는 없다.
특별법 열에는 조례 칸이 없으며, 대신 옆으로 뻗어 다른 열의 값을 덮어쓴다.

---

## 파일

```
lawkit/
  check.js              CLI — 대지 조건으로 검토 1건
  resolver.js           판정 엔진. 순수 함수, 룰은 인자로 받는다
  resolver.test.js      회귀 30건
  rules/*.json          검수된 룰. 계산은 이것만 읽는다
  laws.py               수집 대상 법령 22건 목록
  fetch.py              법제처 Open API — 본문/별표 수집, 개정 감지
  test_fetch.py         파서 22건 (네트워크 없이 돈다)
  data/                 수집물 (gitignore)
```

## 명령

```bash
# 검토 1건 — 자곡동 271-4
node lawkit/check.js --대지면적 331 --용도지역 자연녹지지역 --시도 서울특별시 \
                     --구역 개발제한구역,취락지구 --용도 근린생활시설 --자격 지정당시거주자

# 테스트 (JS 30 + Python 22)
node --test lawkit/*.test.js          # 루트에서는 파일을 명시해야 한다
cd lawkit && python3 -m unittest test_fetch

# 법령 수집 / 개정 감지 — OC 는 국가법령정보 오픈API 신청 이메일의 @ 앞부분
python3 lawkit/fetch.py fetch --oc <아이디>
python3 lawkit/fetch.py check --oc <아이디>
```

---

## 현재 커버리지

**계산됨**
- 서울 + 용도지역 7종 (자연녹지·생산녹지·보전녹지·1~3종일반주거·준주거) + 구역 없음
- 개발제한구역 + 취락지구 (전국)

**보류(`NEEDS_REVIEW`)**
- 서울 외 모든 지자체 (조례가 다름)
- 나머지 용도지역 14종
- 차단구역 10종 중 룰이 있는 건 GB 취락지구뿐

참고로 국토교통부 「한국건축규정」 별표2 는 건축허가 시 확인해야 할 법령을
**140개(입지관련 68개)** 로 정의한다. 우리 `차단구역` 은 10개다.
모르면 보류하는 구조가 필수인 이유가 이 숫자다.

## 미검증 — 반드시 인지할 것

**이 저장소의 법령 수치는 조문 원문으로 대조되지 않았다.** 검색 교차확인 기준이다.
`law.go.kr`, `open.law.go.kr`, `apis.data.go.kr`, `eum.go.kr` 이 세션 egress 정책에
차단되어 있어 원문을 열지 못했다.

- `rules/gb-chwirak.json` — **미검수**, `기준시행일` 미기록
- `resolver.js` 의 `용도지역시드` — **미검수**
- **지하층이 연면적 캡에 산입되는지 미확정** — 조문 문언 기준으로 "포함"으로 보고 있다.
  관할 구청 질의 필요. 결과가 100㎡ 넘게 갈린다.
- GB 시행령 별표1 제5호 라목의 **근생 허용 업종 목록** 미확보
- 271-4 대지면적 331㎡ — 인근 271-21 과 동일 수치라 토지대장 대조 필요

## 다음 할 일

1. **GB 시행령 별표1 원문 확보** → 지하층·근생 업종 확정, 룰 `검수완료` 전환
2. **한국건축규정 별표2 체크리스트** 확보 → 공식 140개에 우리 파이프라인 매핑
3. 서울시 도시계획조례 별표 → 용도지역 7종 → 21종
4. 검토 결과에 룰 id + 검수 버전 기록 (provenance) → 개정 시 "낡은 검토" 알림
5. 주차·조경·일조 (파이프라인 06단계) — 면적 확정 이후라 마지막

## 뼈대로서의 한계

새 특별법 지구는 **룰 JSON 하나로 붙는다** (엔진 코드 수정 없음, 테스트로 확인).
숫자 판정의 뼈대로는 쓸 수 있다.

다만 이것만으로 법규검토가 완성되지 않는다.

- **대지 형상이 없다.** 일조사선·도로사선·이격거리·건축가능영역은 기하 문제라
  숫자 룰로 풀리지 않는다. 별도 엔진이 필요하고, 옆에 세워 붙여야 한다.
- **단방향 1회 통과다.** 실제 검토는 면적↔주차↔조경이 순환한다.
- **증축·기존건축물은 스코프 밖.** 허가시점 법령과 기존불적합 판단이 필요하다.

## 브랜치

작업 브랜치는 `claude/jagok-dong-coverage-ratio-bgky5v`.
